{
    "builds": [{
        "src": "api/index.py",
        "use": "@vercel/python",
        "config": {"maxLambdaSize": "15md"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index.py"
        }
    ]
}