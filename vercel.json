{
    "builds": [{
        "src": "api.py",
        "use": "@vercel/python",
        "config": {"maxLambdaSize": "15md"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api.py"
        }
    ]
}